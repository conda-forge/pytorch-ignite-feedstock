{% set name = "pytorch-ignite" %}
{% set version = "0.4.10" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pytorch-ignite-{{ version }}.tar.gz
  sha256: b56609774ada5a140da3975b6bee9fdce234b1db74567f7cffce0020ff8aa7ad

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - python >=3.6
    - pytorch >=1.3,<2

test:
  imports:
    - ignite
    - ignite.base
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/pytorch/ignite
  summary: A lightweight library to help with training neural networks in PyTorch.
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - knc6
